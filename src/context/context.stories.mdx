<Meta title="Shared code/Context" />

# Context

Context provides a way to pass data through the component tree without having to pass props down manually at every level.

## ColorScheme

A custom React context to set and retrieve the color scheme.

## DeviceType

A custom React context to set and retrieve the device type.
