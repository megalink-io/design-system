import { Meta, Description, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import DesignPreset from '.';

<Meta title="Components/link-page/other/DesignPreset" component={DesignPreset} />

# DesignPreset

<Description of={DesignPreset} />

export const Template = args => <DesignPreset {...args} />;

<Preview withToolbar>
  <Story
    name="Default"
    args={{
      selected: false,
      design: {
        document: {
          backgroundVariant: 'color',
          backgroundColor: '#fff',
          backgroundLineGradient: '',
        },
        elements: {
          default: {
            alignment: 'center',
            fontFamily: 'poppins',
            fontColor: '#fff',
            shape: 'angular',
            backgroundVariant: 'filled',
            backgroundColor: '#313131',
            shadowVariant: 'none',
            shadowColor: '',
          },
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

## Code example

```tsx
import { DesignPreset } from '@mds/components/link-page/other';
```

```tsx
<DesignPreset
  selected={false}
  onClick={() => handleClick()}
  design={{
    document: {
      backgroundVariant: 'color',
      backgroundColor: '#fff',
    },
    elements: {
      default: {
        alignment: 'center',
        fontFamily: 'poppins',
        fontColor: '#fff',
        shape: 'angular',
        backgroundVariant: 'filled',
        backgroundColor: '#313131',
        shadowVariant: 'none',
      },
    },
  }}
/>
```

## Props table

<ArgsTable of={DesignPreset} />
