import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import {
  SingleFormLoadingPreview,
  SignupPreview,
  MainAppLoadingPreview,
  MediaLibraryPreview,
  MediaFilePreview,
} from './Page.stories.tsx';
import Page from '.';

<Meta title="Components/core/layout/Page" component={Page} />

# Page

<Description of={Page} />

## Code example

```tsx
import { Page } from '@mds/components/core/layout';
```

```tsx
<Page variant="main_app">...</Page>
```

## Props table

<ArgsTable of={Page} />

## Example screens

### Single form loading page

<Canvas className="fullframe" withToolbar>
  <Story name="Single form loading" parameters={{ noPadding: true }}>
    <SingleFormLoadingPreview />
  </Story>
</Canvas>

### Signup page

<Canvas className="fullframe" withToolbar>
  <Story name="Signup" parameters={{ noPadding: true }}>
    <SignupPreview />
  </Story>
</Canvas>

### Main app loading page

> Unfortunately the preview does not work here.

<Canvas className="fullframe" withToolbar>
  <Story name="Main app loading" parameters={{ noPadding: true }}>
    <MainAppLoadingPreview />
  </Story>
</Canvas>

### Media library page

> Unfortunately the preview does not work here.

<Canvas className="fullframe" withToolbar>
  <Story name="Media library" parameters={{ noPadding: true }}>
    <MediaLibraryPreview />
  </Story>
</Canvas>

### Media file page

> Unfortunately the preview does not work here.

<Canvas className="fullframe" withToolbar>
  <Story name="Media file" parameters={{ noPadding: true }}>
    <MediaFilePreview />
  </Story>
</Canvas>
