import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Logo from '.';

<Meta title="Components/core/graphics/Logo" component={Logo} />

# Logo

<Description of={Logo} />

export const Template = (args) => <Logo {...args} />;

export const getProps = (id) => ({
  variant: id,
  size: 'medium',
});

<Canvas withToolbar>
  <Story name="Logo" args={getProps('logo')}>
    {Template.bind({})}
  </Story>
  <Story name="Icon" args={getProps('icon')}>
    {Template.bind({})}
  </Story>
  <Story name="Font" args={getProps('font')}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { Logo } from '@mds/components/core/graphics';
```

```tsx
<Logo variant="logo" size="medium" />
```

## Props table

<ArgsTable of={Logo} />

> If the `fontColor` property is not specified, the color automatically adapts to the current color scheme.
