import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import StatusLight from '.';

<Meta title="Components/core/status/StatusLight" component={StatusLight} />

# StatusLight

<Description of={StatusLight} />

export const Template = (args) => <StatusLight {...args} />;

<Canvas withToolbar>
  <Story name="Positive" args={{ color: 'green' }}>
    {Template.bind({})}
  </Story>
  <Story name="Negative" args={{ color: 'red' }}>
    {Template.bind({})}
  </Story>
  <Story name="Disabled" args={{ color: 'green', disabled: true }}>
    {Template.bind({})}
  </Story>
  <Story name="With label" args={{ color: 'blue', label: 'Active' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { StatusLight } from '@mds/components/core/status';
```

```tsx
<StatusLight color="green" />
```

## Props table

<ArgsTable of={StatusLight} />
