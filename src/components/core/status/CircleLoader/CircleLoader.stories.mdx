import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import CircleLoader from '.';

<Meta title="Components/core/status/CircleLoader" component={CircleLoader} />

# CircleLoader

<Description of={CircleLoader} />

export const Template = (args) => <CircleLoader {...args} />;

export const getProps = (id) => ({
  size: id,
  color: 'blue',
});

<Canvas withToolbar>
  <Story name="Large" args={getProps('large')}>
    {Template.bind({})}
  </Story>
  <Story name="Medium" args={getProps('medium')}>
    {Template.bind({})}
  </Story>
  <Story name="Small" args={getProps('small')}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { CircleLoader } from '@mds/components/core/status';
```

```tsx
<CircleLoader type="large" color="colorful" />
```

## Props table

<ArgsTable of={CircleLoader} />
