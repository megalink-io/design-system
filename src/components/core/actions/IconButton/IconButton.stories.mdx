import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import { DeleteSVG, GearSVG, DownloadSVG, ArrowRightSVG, AlarmSVG } from 'icons';
import IconButton from '.';

<Meta title="Components/core/actions/IconButton" component={IconButton} />

# IconButton

<Description of={IconButton} />

export const Template = (args) => <IconButton {...args} />;

export const getProps = (id) => ({
  icon: <DeleteSVG />,
  label: 'Delete',
  variant: id,
  color: 'blue',
});

<Canvas withToolbar>
  <Story name="Primary" args={getProps('primary')}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary" args={getProps('secondary')}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary" args={getProps('tertiary')}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { IconButton } from '@mds/components/core/actions';
```

```tsx
<IconButton icon={<DeleteSVG />} label="Delete" variant="tertiary" type="button" onClick={() => handleClick()} />
<IconButton icon={<GearSVG />} label="Settings" variant="tertiary" type="link" to="/settings" />
```

## Props table

<ArgsTable of={IconButton} withToolbar />

## More stories

### Notifications

<Canvas withToolbar>
  <Story
    name="Notifications"
    args={{
      icon: <AlarmSVG />,
      label: 'Notifications',
      variant: 'tertiary',
      type: 'link',
      to: '/notifications',
      counter: 3,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Internal link

<Canvas withToolbar>
  <Story
    name="Internal link"
    args={{
      icon: <GearSVG />,
      label: 'Settings',
      variant: 'tertiary',
      type: 'link',
      to: '/settings',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### External link

<Canvas withToolbar>
  <Story
    name="External link"
    args={{
      icon: <ArrowRightSVG />,
      label: 'Link',
      variant: 'tertiary',
      type: 'link',
      href: 'https://www.example.com/settings',
      target: '_blank',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Download link

<Canvas withToolbar>
  <Story
    name="Download link"
    args={{
      icon: <DownloadSVG />,
      label: 'Download',
      variant: 'tertiary',
      type: 'link',
      href: 'https://www.example.com/invoice.pdf',
      download: 'invoice.pdf',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
