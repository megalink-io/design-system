import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import ActionButton from '.';

<Meta title="Components/core/actions/ActionButton" component={ActionButton} />

# ActionButton

<Description of={ActionButton} />

export const Template = (args) => <ActionButton {...args} />;

export const getProps = (id) => ({
  label: 'Button',
  variant: id,
  color: 'blue',
});

<Canvas withToolbar>
  <Story name="Primary" args={getProps('primary')}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary" args={getProps('secondary')}>
    {Template.bind({})}
  </Story>
  <Story name="Tertiary" args={getProps('tertiary')}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { ActionButton } from '@mds/components/core/actions';
```

```tsx
<ActionButton
  label="Button"
  variant="primary"
  color="blue"
  type="button"
  onClick={() => handleClick()}
/>
```

## Props table

<ArgsTable of={ActionButton} withToolbar />

## More stories

### Internal link

<Canvas withToolbar>
  <Story
    name="Internal link"
    args={{
      label: 'Settings',
      variant: 'secondary',
      type: 'link',
      to: '/settings',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### External link

<Canvas withToolbar>
  <Story
    name="External link"
    args={{
      label: 'Signup',
      variant: 'primary',
      color: 'gray',
      type: 'link',
      href: 'https://www.example.com/signup',
      target: '_blank',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Download link

<Canvas withToolbar>
  <Story
    name="Download link"
    args={{
      label: 'Download',
      variant: 'tertiary',
      type: 'link',
      href: 'https://www.example.com/invoice.pdf',
      download: 'invoice.pdf',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
