import { Meta, Description, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { ShareSVG, GearSVG, DownloadSVG, ArrowRightSVG } from 'icons';
import FloatingButton from '.';

<Meta title="Components/core/actions/FloatingButton" component={FloatingButton} />

# FloatingButton

<Description of={FloatingButton} />

export const Template = args => <FloatingButton {...args} />;

<Canvas className="fullframe" withToolbar>
  <Story name="Share button" args={{ icon: <ShareSVG />, label: 'Share', color: 'blue' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { FloatingButton } from '@mds/components/core/actions';
```

```tsx
<FloatingButton icon={<DeleteSVG />} label="Delete" color="blue" type="button" onClick={() => handleClick()} />
<FloatingButton icon={<GearSVG />} label="Settings" color="blue" type="link" to="/settings" />
```

## Props table

<ArgsTable of={FloatingButton} withToolbar />

## More stories

### Internal link

<Canvas className="fullframe" withToolbar>
  <Story
    name="Internal link"
    args={{ icon: <GearSVG />, color: 'blue', label: 'Settings', type: 'link', to: '/settings' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### External link

<Canvas className="fullframe" withToolbar>
  <Story
    name="External link"
    args={{
      icon: <ArrowRightSVG />,
      color: 'blue',
      label: 'Link',
      type: 'link',
      href: 'https://www.example.com/link',
      target: '_blank',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Download link

<Canvas className="fullframe" withToolbar>
  <Story
    name="Download link"
    args={{
      icon: <DownloadSVG />,
      color: 'blue',
      label: 'Download',
      type: 'link',
      href: 'https://www.example.com/invoice.pdf',
      download: 'invoice.pdf',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
