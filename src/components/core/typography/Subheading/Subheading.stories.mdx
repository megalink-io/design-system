import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import { text } from '@storybook/addon-knobs';
import Subheading from '.';

<Meta title="Components/core/typography/Subheading" component={Subheading} />

# Subheading

A subheading divides the content of a section and can appear as often as you like.

<Preview withToolbar>
  <Story name="Default">
    <Subheading children={text('Children', 'This is a subheading', 'default')} />
  </Story>
</Preview>

## Code example

```tsx
import { Subheading } from '@mds/components/core/typography';
```

```tsx
<Subheading>This is a subheading</Subheading>
```

## Props table

<Props of={Subheading} />
