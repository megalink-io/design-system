import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Subheading from '.';

<Meta title="Components/core/typography/Subheading" component={Subheading} />

# Subheading

<Description of={Subheading} />

export const Template = (args) => <Subheading {...args} />;

<Canvas withToolbar>
  <Story name="Default" args={{ children: 'This is a subheading' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { Subheading } from '@mds/components/core/typography';
```

```tsx
<Subheading>This is a subheading</Subheading>
```

## Props table

<ArgsTable of={Subheading} />
