import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Detail from '.';

<Meta title="Components/core/typography/Detail" component={Detail} />

# Detail

<Description of={Detail} />

export const Template = (args) => <Detail {...args} />;

<Canvas withToolbar>
  <Story name="Default" args={{ children: 'This is a detail' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { Detail } from '@mds/components/core/typography';
```

```tsx
<Detail>This is a detail</Detail>
```

## Props table

<ArgsTable of={Detail} />
