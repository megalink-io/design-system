import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import { ActionButton } from 'components/core/actions';
import { Title, Detail } from 'components/core/typography';
import NavBar from '.';

<Meta title="Components/core/navigation/NavBar" component={NavBar} />

# NavBar

<Description of={NavBar} />

export const Template = (args) => <NavBar {...args} />;

<Canvas withToolbar>
  <Story
    name="Media library"
    args={{
      children: (
        <>
          <Title>Media library</Title>
          <Detail>12 % of 0.5 GB used</Detail>
          <ActionButton
            label="Upload"
            variant="primary"
            color="blue"
            type="button"
            onClick={() => {}}
          />
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { NavBar } from '@mds/components/core/navigation';
```

```tsx
<NavBar>
  <Title>Media library</Title>
  <Detail>12 % of 0.5 GB used</Detail>
  <ActionButton
    label="Upload"
    variant="primary"
    color="blue"
    type="button"
    onClick={() => handleUpload()}
    loading={loading}
  />
</NavBar>
```

## Props table

<ArgsTable of={NavBar} />
