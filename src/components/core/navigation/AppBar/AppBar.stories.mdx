import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import { IconButton } from 'components/core/actions';
import { Title, Detail } from 'components/core/typography';
import { AlarmSVG, GearSVG } from 'icons';
import AppBar from '.';

<Meta title="Components/core/navigation/AppBar" component={AppBar} />

# AppBar

A top app bar that displays information and actions related to the current page.

<Preview className="fullframe" withToolbar>
  <Story name="Home">
    <AppBar>
      <Title color="white">Home</Title>
      <IconButton
        icon={<AlarmSVG />}
        label="Notifications"
        variant="tertiary"
        color="white"
        type="link"
        to="/notifications"
        counter={7}
      />
      <IconButton
        icon={<GearSVG />}
        label="Settings"
        variant="tertiary"
        color="white"
        type="link"
        to="/settings"
      />
    </AppBar>
  </Story>
</Preview>

## Code example

```tsx
import { AppBar } from '@mds/components/core/navigation';
```

```tsx
<AppBar>
  <Title color="white">Home</Title>
  <IconButton
    icon={<AlarmSVG />}
    label="Notifications"
    variant="tertiary"
    color="white"
    type="link"
    to="/notifications"
    counter={7}
  />
  <IconButton
    icon={<GearSVG />}
    label="Settings"
    variant="tertiary"
    color="white"
    type="link"
    to="/settings"
  />
</AppBar>
```

## Props table

<Props of={AppBar} />

## More stories

### Media library

<Preview className="fullframe" withToolbar>
  <Story name="Media library">
    <AppBar>
      <Title color="white">Media library</Title>
      <Detail color="white">12 % of 0.5 GB used</Detail>
    </AppBar>
  </Story>
</Preview>
