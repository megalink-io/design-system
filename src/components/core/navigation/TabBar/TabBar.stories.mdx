import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import { IconButton } from 'components/core/actions';
import { HouseSVG, ListSVG, FolderSVG } from 'icons';
import TabBar from '.';

<Meta title="Components/core/navigation/TabBar" component={TabBar} />

# TabBar

<Description of={TabBar} />

export const Template = (args) => <TabBar {...args} />;

<Canvas className="fullframe" withToolbar>
  <Story
    name="Default"
    args={{
      children: (
        <>
          <IconButton
            icon={<HouseSVG />}
            label="Home"
            variant="tertiary"
            color="gray"
            type="link"
            to="/"
            exact
          />
          <IconButton
            icon={<ListSVG />}
            label="Link pages"
            variant="tertiary"
            color="gray"
            type="link"
            to="/link-pages"
          />
          <IconButton
            icon={<FolderSVG />}
            label="Media library"
            variant="tertiary"
            color="gray"
            type="link"
            to="/media-library"
          />
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { TabBar } from '@mds/components/core/navigation';
```

```tsx
<TabBar>
  <IconButton
    icon={<HouseSVG />}
    label="Home"
    variant="tertiary"
    color="gray"
    type="link"
    to="/"
    exact
  />
  <IconButton
    icon={<ListSVG />}
    label="Link pages"
    variant="tertiary"
    color="gray"
    type="link"
    to="/link-pages"
  />
  <IconButton
    icon={<FolderSVG />}
    label="Media library"
    variant="tertiary"
    color="gray"
    type="link"
    to="/media-library"
  />
</TabBar>
```

## Props table

<ArgsTable of={TabBar} />
