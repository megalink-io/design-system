import { Meta, Description, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { IconButton } from 'components/core/actions';
import { HouseSVG, ListSVG, FolderSVG, NightSVG, AlarmSVG, GearSVG } from 'icons';
import SideBar from '.';

<Meta title="Components/core/navigation/SideBar" component={SideBar} />

# SideBar

<Description of={SideBar} />

export const Template = args => <SideBar {...args} />;

<Canvas className="fullframe" withToolbar>
  <Story
    name="Default"
    args={{
      topMenu: (
        <>
          <IconButton
            icon={<HouseSVG />}
            label="Home"
            variant="tertiary"
            color="white"
            type="link"
            to="/"
            exact
          />
          <IconButton
            icon={<ListSVG />}
            label="Link pages"
            variant="tertiary"
            color="white"
            type="link"
            to="/link-pages"
          />
          <IconButton
            icon={<FolderSVG />}
            label="Media library"
            variant="tertiary"
            color="white"
            type="link"
            to="/media-library"
          />
        </>
      ),
      bottomMenu: (
        <>
          <IconButton
            icon={<NightSVG />}
            label="Color scheme"
            variant="tertiary"
            color="white"
            type="button"
            onClick={() => {}}
          />
          <IconButton
            icon={<AlarmSVG />}
            label="Notifications"
            variant="tertiary"
            color="white"
            type="link"
            to="/notifications"
            counter={7}
          />
          <IconButton
            icon={<GearSVG />}
            label="Settings"
            variant="tertiary"
            color="white"
            type="link"
            to="/settings"
          />
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { SideBar } from '@mds/components/core/navigation';
```

```tsx
<SideBar
  topMenu={
    <>
      <IconButton
        icon={<HouseSVG />}
        label="Home"
        variant="tertiary"
        color="white"
        type="link"
        to="/"
        exact
      />
      <IconButton
        icon={<ListSVG />}
        label="Link pages"
        variant="tertiary"
        color="white"
        type="link"
        to="/link-pages"
      />
      ...
    </>
  }
  bottomMenu={
    <>
      <IconButton
        icon={colorScheme === 'light' ? <NightSVG /> : <SunSVG />}
        label="Color scheme"
        variant="tertiary"
        color="white"
        type="button"
        onClick={changeColorScheme}
      />
      <IconButton
        icon={<AlarmSVG />}
        label="Notifications"
        variant="tertiary"
        color="white"
        type="link"
        to="/notifications"
        counter={7}
      />
      ...
    </>
  }
/>
```

## Props table

<ArgsTable of={SideBar} />
