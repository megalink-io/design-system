import { Meta, Preview, Story, Props } from '@storybook/addon-docs/blocks';
import { SideBarPreview } from './SideBar.stories.tsx';
import SideBar from '.';

<Meta title="Components/core/navigation/SideBar" component={SideBar} />

# SideBar

A sidebar that allows users to navigate the content of the app using a single-level icon navigation.

<Preview className="fullframe" withToolbar>
  <Story name="Default">
    <SideBarPreview />
  </Story>
</Preview>

## Code example

```tsx
import { SideBar } from '@mds/components/core/navigation';
```

```tsx
<SideBar
  topMenu={
    <>
      <IconButton
        icon={<HouseSVG />}
        label="Home"
        variant="tertiary"
        color="white"
        type="link"
        to="/"
        exact
      />
      <IconButton
        icon={<ListSVG />}
        label="Link pages"
        variant="tertiary"
        color="white"
        type="link"
        to="/link-pages"
      />
      ...
    </>
  }
  bottomMenu={
    <>
      <IconButton
        icon={colorScheme === 'light' ? <NightSVG /> : <SunSVG />}
        label="Color scheme"
        variant="tertiary"
        color="white"
        type="button"
        onClick={changeColorScheme}
      />
      <IconButton
        icon={<AlarmSVG />}
        label="Notifications"
        variant="tertiary"
        color="white"
        type="link"
        to="/notifications"
        counter={7}
      />
      ...
    </>
  }
/>
```

## Props table

<Props of={SideBar} />
