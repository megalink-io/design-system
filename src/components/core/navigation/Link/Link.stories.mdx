import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import { SparklesSVG, ShoppingCartSVG, SmilingFaceWithSunglassesSVG } from 'openmoji';
import Link from '.';

<Meta title="Components/core/navigation/Link" component={Link} />

# Link

<Description of={Link} />

> In addition to simple text, the link component can also be applied to graphics or other elements.

export const Template = (args) => <Link {...args} />;

<Canvas withToolbar>
  <Story
    name="Internal link"
    args={{
      to: '/settings',
      children: 'Settings',
      underlined: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="External link"
    args={{
      href: 'https://www.example.com/privacy-policy',
      target: '_blank',
      children: 'Privacy policy',
      underlined: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Graphics"
    args={{
      to: '/openmoji',
      children: (
        <>
          <SparklesSVG style={{ height: '22rem' }} />
          <ShoppingCartSVG style={{ height: '22rem' }} />
          <SmilingFaceWithSunglassesSVG style={{ height: '22rem' }} />
        </>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Download link"
    args={{
      href: 'https://www.example.com/invoice.pdf',
      download: 'invoice.pdf',
      children: 'Download',
      underlined: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { Link } from '@mds/components/core/navigation';
```

```tsx
<Link to="/settings">Settings</Link>
<Link href="https://www.example.com/privacy-policy" target="_blank" underlined>Privacy policy</Link>
```

## Props table

<ArgsTable of={Link} />
