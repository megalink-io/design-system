import {
  Meta,
  Description,
  Canvas,
  Story,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import { IconButton } from 'components/core/actions';
import { HouseSVG, ListSVG, FolderSVG } from 'icons';
import Tabs from '.';

<Meta title="Components/core/navigation/Tabs" component={Tabs} />

# Tabs

<Description of={Tabs} />

export const Template = (args) => <Tabs {...args} />;

<Canvas withToolbar>
  <Story
    name="Default"
    args={{
      tabs: [
        {
          label: 'Tab one',
          to: '/tab-one',
        },
        {
          label: 'Tab two',
          to: '/tab-two',
        },
        {
          label: 'Tab three',
          to: '/tab-three',
        },
        {
          label: 'Tab four',
          to: '/tab-four',
        },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { Tabs } from '@mds/components/core/navigation';
```

```tsx
<Tabs
  tabs={[
    {
      label: 'Tab one',
      to: '/tab-one',
    },
    {
      label: 'Tab two',
      to: '/tab-two',
    },
    {
      label: 'Tab three',
      to: '/tab-three',
    },
  ]}
/>
```

## Props table

<ArgsTable of={Tabs} />
