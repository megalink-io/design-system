import { Meta, Description, ArgsTable } from '@storybook/addon-docs/blocks';
import { useForm } from 'hooks';
import Form from '.';

<Meta title="Components/core/inputs/Form" component={Form} />

# Form

<Description of={Form} />

> The `type` property of the button for submitting the form must have the value `submit`.

## Key features

- Removes the browser's native form validation
- Prevents the browser default behavior when submitting

## Code example

```tsx
import { Form } from '@mds/components/core/inputs';
```

```tsx
<Form onSubmit={() => handleSubmit()}>...</Form>
```

## Props table

<ArgsTable of={Form} />
