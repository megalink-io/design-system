import { Meta, Description, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { useForm } from 'hooks';
import InputError from '.';

<Meta title="Components/core/inputs/InputError" component={InputError} />

# InputError

<Description of={InputError} />

export const Template = args => <InputError {...args} />;

<Canvas withToolbar>
  <Story name="Default" args={{ text: 'This is an example error text.' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { InputError } from '@mds/components/core/inputs';
```

```tsx
<InputError error="This is an example error text." />
```

## Props table

<ArgsTable of={InputError} />
