import { Meta, Description, Canvas, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { useForm } from 'hooks';
import InputLabel from '.';

<Meta title="Components/core/inputs/InputLabel" component={InputLabel} />

# InputLabel

<Description of={InputLabel} />

export const Template = args => <InputLabel {...args} />;

export const props = {
  label: 'Example text',
  required: true,
};

<Canvas withToolbar>
  <Story name="Label" args={{ ...props, as: 'label', htmlFor: 'example' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Code example

```tsx
import { InputLabel } from '@mds/components/core/inputs';
```

```tsx
<InputLabel label="Example text" htmlFor="example" required />
```

## Props table

<ArgsTable of={InputLabel} />

## More stories

### Span

<Canvas withToolbar>
  <Story name="Span" args={{ ...props, as: 'span' }}>
    {Template.bind({})}
  </Story>
</Canvas>
